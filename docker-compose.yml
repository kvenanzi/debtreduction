

services:
  web:
    image: ghcr.io/kvenanzi/debtreduction:latest
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - instance-data:/app/instance
    restart: unless-stopped

volumes:
  instance-data:
    driver: local
